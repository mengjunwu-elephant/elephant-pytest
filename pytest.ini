# pytest.ini
[pytest]
disable_test_id_escaping_and_forfeit_all_rights_to_community_support = True
addopts = -s
markers =
    hw: 需要真实硬件连接（机械臂/串口），未连接时可跳过 -m "not hw"
    smoke: 冒烟用例，用于快速验证
    regression: 回归用例