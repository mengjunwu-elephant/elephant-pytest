# Modbus 原型代码

该目录只保存准备拆分为独立项目的 Modbus 原型代码。

当前文件：

```text
pro450_modbus.py
```

目标独立项目：

```text
mycobot450-modbus
```

这里的代码不应长期作为正式库维护。拆分完成后，`elephant-pytest` 应通过 `requirements.txt` 依赖独立项目的 tag 版本。

