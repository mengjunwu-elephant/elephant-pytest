# elephant-pytest Cursor 规则（优化版）

## 一、项目目标
- 本仓库是 **pymycobot 实机自动化测试工程**：`pytest` + `allure-pytest` + `openpyxl`（Excel 驱动）。
- 与选臂/连接相关改动，统一走 `arm_registry.py` + `arms.json`，避免在测试文件里散落解析逻辑。

## 二、沟通与输出
- 对用户解释、代码注释、文档说明优先使用 **简体中文**。
- 修改后优先给出“改了什么 + 为什么改”两句式说明；涉及锁、内存、寄存器或硬件时明确风险边界。

## 三、目录职责（必须遵守）
- `testcases/*`：按产品线组织测试，倾向单 API/单场景单文件 `test_*.py`。
- `common1/`：通用能力（`logger`、`test_data_handler`、`assert_utils`）。
- `settings.py`：各产品线 `*Base`、测试数据文件、超时与等待策略。
- `arm_registry.py` + `arms.json`：臂注册、连接模式、构建设备、用例根路径。
- 根 `conftest.py`：仅保留全局 pytest 会话逻辑（CLI 选项、marker 自动化），不放产品线 `device` 细节。
- `testcases/<产品>/conftest.py`：该产品线的 `device` fixture 与资源生命周期。

## 四、测试开发基线
- 数据读取：优先 `get_test_data_from_excel(Base.TEST_DATA_FILE, sheet, required_columns=...)`。
- 测试结构：`@pytest.mark.parametrize` + `allure.feature/story` + `allure.step`。
- 断言策略：浮点与容差优先 `assert_utils.assert_almost_equal`；关键输入输出建议 `allure.attach`。
- 新增公共函数或 fixture 建议补全类型提示（Type Hints）。
- 严禁臆造 pymycobot API；以官方文档和本机已安装源码为准。

## 五、实机安全红线
- 禁止无上限等待运动结束；统一使用各 `*Base.wait(...)` 的超时机制。
- 禁止在同一台实机上随意并发多进程共连（如盲目使用 `pytest-xdist`）。
- 禁止伪造硬件能力、固件行为、通信结果。

## 六、连接与选臂优先级（Pro 450）
- IP 解析：`--elephant-ip` > 环境变量 (`MYCOBOT450_IP` / `Mycobot450_IP`) > `arms.json.default_ip` > `settings.DEFAULT_MYCOBOT450_IP`。
- 臂选择：`--elephant-arm` > `ELEPHANT_ARM` > `arms.json.default_arm`。

## 七、推荐验证动作
- 改动 `settings.py`、任意 `conftest.py`、`arm_registry.py`、`arms.json` 后，先执行：
  - `pytest testcases --collect-only`
- 改动某产品线用例后，至少执行：
  - `pytest path/to/test_file.py --collect-only`

## 八、与 Skill 的分工
- 具体“如何写 Excel 用例”请调用 `elephant-pytest-case-authoring`。
- 具体“如何改选臂/会话解析”请调用 `elephant-pytest-registry-session`。
- 规则文件只保留跨模块强约束，不重复 Skill 的细节步骤。
