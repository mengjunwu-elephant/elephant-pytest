# elephant-pytest — Cursor 项目规则

## 项目定位
- **机械臂 Python 库（pymycobot）自动化测试**：实机/控制器通过 `Pro450Client`，栈为 **pytest + allure-pytest + openpyxl（Excel 驱动）**。
- 说明文档：`README.md`、`docs/EXCEL_TEST_DATA.md`。

## 语言与沟通
- 对用户说明、注释、文档优先使用 **简体中文**。

## 目录与职责
- `testcases/mycobot_450/`、`testcases/mycobot450_pro_gripper/`：用例；单 API/场景倾向单文件 `test_*.py`。
- `common1/`：日志、`test_data_handler`、`assert_utils` 等公共代码。
- `settings.py`：`Mycobot450Base`、Excel 路径、`resolve_mycobot450_ip()`、`wait()` 超时逻辑；**勿臆造 pymycobot 未文档化的 API**。
- 根目录 `conftest.py`：共享 `mycobot_ip`、`device`（module 级，默认 teardown 仅 `mc.close()`）；`testcases` 下用例默认打 `hardware`。
- 若模块需 **夹爪复位 / 参数恢复 / 特殊 setup**，在该模块内 **重新定义 `device` fixture** 覆盖根 conftest（pytest 就近覆盖）。

## 用例编写约定
- 数据：`cases = get_test_data_from_excel(..., sheet_name)`，sheet 名与 Excel 一致；可选 `required_columns=` 做强校验。
- 结构：`@pytest.mark.parametrize` + `@allure.feature` / `@allure.story` + `with allure.step`；关键断言处可 `allure.attach`。
- 标记：在 `pytest.ini` 已注册 `hardware`、`slow`、`smoke`；新增长运动/多等待用例考虑 `@pytest.mark.slow`，首批冒烟考虑 `smoke`。
- **类型**：新代码对公共函数、fixture 返回值尽量加 **类型提示**。

## 环境与安全（实机）
- IP：`MYCOBOT450_IP` 或 `Mycobot450_IP`；Debug：`MYCOBOT450_DEBUG`；运动等待：`MYCOBOT450_MOVE_TIMEOUT_SEC`（见 `README.md`）。
- **禁止**在测试里写死死循环等待运动结束而不设上限；应使用 `Mycobot450Base.wait()` 或同等超时策略。
- 真机单实例时 **不要** 引入多进程并发跑同一臂（如轻率使用 `pytest-xdist` 共连一台机器人）。
- 不伪造硬件行为；控制器/固件能力以官方文档与本地 pymycobot 源码为准。

## 依赖
- 本地开发：`pip install -e .` 安装 pymycobot 时，避免与 `requirements.txt` 中 PyPI 行冲突（见 README）。
- 生成 Allure 数据需安装 `allure-pytest`，命令行加 `--alluredir=allure-results`。

## AI 协助时的偏好
- 小步修改：优先改与需求直接相关的文件，避免无关大重构。
- 修改共享逻辑（`settings.py`、`conftest.py`、`test_data_handler.py`）后，提示运行 `pytest testcases --collect-only` 做快速校验。
